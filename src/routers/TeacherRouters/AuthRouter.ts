import express from 'express'
import { TeacherController } from '../../controllers/Teacher/AuthController'

const {
  Teachers

} = TeacherController()

const router = express.Router()

router.post('/TeacherSignup',)
router.post('/TeacherLogin',)